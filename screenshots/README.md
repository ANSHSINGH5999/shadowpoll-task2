# Screenshots

- `compile-output.png` — output of `compact compile` showing the generated circuits (`contract/src/managed/shadow_poll/`)
- `test-output.png` — `npm test` passing suite output
- `deploy-output.png` — `npm run deploy:preview` output showing the deployed contract address
